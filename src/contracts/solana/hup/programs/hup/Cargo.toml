[package]
name = "hup"
version = "0.1.0"
description = "Hup on Solana - log-first social core"
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]
name = "hup"

[features]
default = []
cpi = ["no-entrypoint"]
no-entrypoint = []
no-idl = []
no-log-ix-name = []
idl-build = ["anchor-lang/idl-build"]

[dependencies]
anchor-lang = "0.31.1"
